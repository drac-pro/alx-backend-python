#!/usr/bin/python3
"""Defines a type anotated function add"""


def add(a: float, b: float) -> float:
    """adds two floating point numbers
    Args:
        a(float): num one
        b(float): num two
    Return:
        float: sum of a + b
    """
    return a + b
